class User {
  final String name;
  final String userSchool;
  final String image;
  bool isSelected;
  final double downloadCount;

  User(this.name, this.userSchool, this.image, this.isSelected, this.downloadCount);
}
